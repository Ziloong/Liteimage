# 2026-04-20 工作日志

## 轻图 Windows 版前端开发

在 `/Users/ziloong/Documents/轻图png-windows/` 创建完整的 React + TypeScript 前端代码：

### 已完成文件
- `src/App.tsx` + `src/App.css` - 主应用组件
- `src/types.ts` - TypeScript 类型定义
- `src/main.tsx` - React 入口
- `src/components/ImageCompress.tsx` + `.css` - 图片压缩页面
- `src/components/VideoToGif.tsx` + `.css` - 视频转GIF页面
- `src/components/Settings.tsx` + `.css` - 设置弹窗
- `index.html`, `package.json`, `vite.config.ts`, `tsconfig.json`

### 技术栈
- React 18 + TypeScript
- Tauri v2 API (`@tauri-apps/api/core`, `@tauri-apps/plugin-dialog`)
- 纯手写 CSS，无框架

### 备注
- rust-backend 已在 `src-tauri/` 创建 Rust 后端代码
- 窗口尺寸设计为 820x580

## 2026-04-20 项目修复与完善

修复轻图 Windows 版（Tauri v2 + React）项目的多处编译问题：

### Rust 后端修复
- 删除 `main.rs` 中不存在的模块引用（`compressor`、`gif_converter`、`video`），只保留实际存在的 `tinypng`
- 修复 `tinypng.rs`：移除未使用的 `serde` 导入，将 `async fn` 改为 `fn`（因为使用 `reqwest::blocking`）
- 修复 `main.rs`：将所有不需要 async 的 command 改为同步函数
- 修复 `ApiTestResult` 类型冲突：`main.rs` 和 `tinypng.rs` 各自定义了同名结构体，添加手动转换

### 前端修复
- `App.tsx`：添加缺失的 `import "./App.css"`，清理未使用的导入
- `vite.config.ts`：修复 `process.env` 在 ESM 模式下不可用的问题
- `package.json`：添加 `tauri` script 命令

### Tauri 配置完善
- `capabilities/default.json`：添加 `core:default`、`dialog:default`、`dialog:allow-open` 权限
- `tauri.conf.json`：添加 `dragDropEnabled: true` 窗口配置
- `resources/README.md`：添加工具二进制文件说明

### 当前状态
- 项目代码已修复完毕，可在 Windows 端执行 `npm install && npm run tauri build` 构建
- 构建前需要将 pngquant.exe、gifski.exe、ffmpeg.exe、ffprobe.exe 放入 `src-tauri/resources/`

## 2026-04-20 Windows 构建成功

### 关键修复
1. **WiX MSI 打包中文编码问题**：WiX 3.14 默认 code page 1252 不支持中文，将 `productName` 改为 "LiteImage"，`title` 改为 "LiteImage"
2. **代理设置**：Windows VM 上有代理 `127.0.0.1:7890`，设置 `HTTP_PROXY`/`HTTPS_PROXY` 后 WiX 和 NSIS 工具可正常下载
3. **文件同步注意**：macOS tar 同步到 Windows 时，`LiteImage-Windows` 和 `轻图png-windows` 是不同目录，需注意目标路径

### 构建产物（已复制到 macOS）
- `build-output/LiteImage_1.0.0_x64-setup.exe` (1.3MB) - NSIS 安装包
- `build-output/LiteImage_1.0.0_x64_en-US.msi` (1.9MB) - MSI 安装包
- `build-output/liteimage.exe` (3.7MB) - 独立可执行文件

### 待排查
- Release exe 双击无反应问题仍未解决（debug 版本已编译但日志为空）
- 之前添加了 logger.rs 模块用于调试
